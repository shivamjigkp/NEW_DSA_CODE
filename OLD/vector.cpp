#include<iostream>
#include<vector>

using namespace std;
/*
 int main(){
    vector<int>shivam;
    vector<int>shivamji(6);
    shivam.size() // give the size of vector
    shivam.resize(8) // resize the vector to 8 also increase cpacity if capicity is smaller 
    shivam.capacity() // capcity >= size capacity increases as 2 ki power 
    shivam.pushback(4) // new element put as 4 in the shivam
    //v.insert(position ,element) method 2 ki v.insert(v.begin()+ 4, element)

    shivam.insert(4,981)
    shivam.begin() //give the begining position
    shivam.end()//give the end position 
    shivam.pop back() // delete the last vector element
    
    shivam.erase() //put the position from begining delete that element
    cout<< v.size();//give the size
    for(int i=0,i<5;i++){
        int element;
        cin>>element ;
        v.pushback(element);
        int ele:shivam
        cout<<ele<<" " ;// ye elelment ko prin karnr ka hai 
        cout<<shivam[1] ;// ye hota hai for iterate karke print karane ke liye


         

    }

*/





    return 0;

}